<h1> State: <%= @state.name %> </h1>
<% @state.shops.each do |s| %>
<ol>
    <li> 
        Name :<%= s.name %> 
        <br>
        <br>
        <%= link_to "Edit a shop", edit_shop_path(s.id) %>
    </li>
</ol>
<% end %>